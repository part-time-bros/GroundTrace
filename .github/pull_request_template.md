## What this changes

<!-- One or two sentences. What is different afterwards? -->

## Why

<!-- The problem, not the patch. If it fixes an issue, link it. -->

## How it was verified

<!-- Be specific. "Tests pass" is the claim this project exists to distrust. -->

- [ ] `pnpm build && pnpm test && pnpm lint && pnpm format:check`
- [ ] Added a test that fails without this change
- [ ] Checked it against the demo (`cd examples/dashboard-demo && pnpm dev`)

<!-- If any box is unticked, say why — an honest gap is more useful than a tick. -->

## Notes

<!-- Judgement calls worth recording? Add them to DECISIONS.md too. -->
